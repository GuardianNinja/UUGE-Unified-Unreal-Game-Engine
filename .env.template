# Environment variables for the Writer-Reviewer Workflow app

# Default prompt used if no prompt is explicitly supplied.
DEFAULT_USER_REQUEST=Draft an engaging product summary for a new sustainability service.

# HTTP server settings
APP_HOST=127.0.0.1
APP_PORT=8000
